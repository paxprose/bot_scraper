{
  "name": "bot_scraper",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "format": "npm run private:format -- --write",
    "check-format": "npm run private:format -- --list-different",
    "private:format": "prettier index.js \"endpoints/*.js\" \"cookies/*.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "GPLv3",
  "dependencies": {
    "axios": "^0.20.0",
    "open": "^7.2.1",
    "puppeteer": "^5.3.0",
    "puppeteer-har": "^1.1.2"
  },
  "devDependencies": {
    "prettier": "^2.1.2"
  }
}
